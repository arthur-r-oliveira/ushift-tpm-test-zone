apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tpm-test-zone

resources:
  - namespace.yaml
  - pod-tpm.yaml # Updated to match your filename

# Using 'labels' instead of deprecated 'commonLabels'
labels:
  - pairs:
      test-type: hardware-root-of-trust
